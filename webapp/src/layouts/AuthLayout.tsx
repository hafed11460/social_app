import { Outlet } from "react-router-dom";



const AuthLayout = () => {
    // const token = true
    // if(token != null) return <Navigate to ='/'/>

    return <Outlet />

}
export default AuthLayout;